# Seeds Directory

This directory contains SQL files for seeding "Dummy Data" or "Initial Content" into school databases.
These files are NOT run automatically during `npm run db:migrate:all`.

## Usage
- `npm run db:seed:all` — Seeds every school database.
- `npm run db:seed` — Seeds the default database from `.env`.
